import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#000000'
    },
    secondary: {
      main: '#FF00FE'
    }
  }
})

export default theme
